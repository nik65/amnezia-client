Readiness is a gate, not a repair routine. A green receipt proves that the
guest marker, selected profile, QEMU guest agent, cloud-init completion, and
absence of candidate credentials were observed. A sealed receipt additionally
proves that the guest has no default route. The controller must separately
prove that QMP/QGA sockets are private Unix sockets and that the host-side
network has not been changed.
